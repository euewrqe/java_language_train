客户端循环发送数据，直到发送886停止
服务端循环接收数据，直到接收到886就停止
用udp协议完成

- 客户端为发送端
发送端的步骤:
1，定义一个DatagramSocket对象
2， 键盘传入数据，并保存到Buffer
3，打包数据和地址DatagramPacket
4，发送数据包
5，关闭

- 服务端为接收端
服务端接收步骤:
1，定义一个DatagramSockeet对象
2，定义一个包用来接收数据
3，接收数据
4， 关闭